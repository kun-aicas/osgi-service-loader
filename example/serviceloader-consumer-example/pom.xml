<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.aicas.osgi.example</groupId>
    <artifactId>example</artifactId>
    <version>${revision}</version>
  </parent>

  <name>OSGi Serviceloader Consumer Example</name>
  <groupId>com.aicas.osgi.example</groupId>
  <artifactId>serviceloader-consumer-example</artifactId>
  <packaging>bundle</packaging>
  <description>
    Example OSGi client bundle that uses the osgi.serviceloader.processor
    extender and loads service implementations through the Java ServiceLoader API.
  </description>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.felix</groupId>
        <artifactId>maven-bundle-plugin</artifactId>
        <version>${maven.bundle.plugin.version}</version>
        <extensions>true</extensions>
        <configuration>
          <instructions>
            <Import-Package>
              org.osgi.framework;version="[1.6,2)",
              com.aicas.osgi.spi.example.spi
            </Import-Package>
            <Bundle-Activator>com.aicas.osgi.spi.example.client.ServiceloaderConsumerExampleActivator</Bundle-Activator>
            <Require-Capability>osgi.serviceloader; filter:="(osgi.serviceloader=com.aicas.osgi.spi.example.spi.SPIProvider)";cardinality:=multiple;resolution:=optional,osgi.extender; filter:="(osgi.extender=osgi.serviceloader.processor)"</Require-Capability>
          </instructions>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-assembly-plugin</artifactId>
        <version>${maven.assembly.plugin.version}</version>
        <executions>
          <execution>
            <id>distribution-assembly</id>
            <phase>package</phase>
            <goals>
              <goal>single</goal>
            </goals>
            <configuration>
              <descriptors>
                <descriptor>src/main/assembly/distribution.xml</descriptor>
              </descriptors>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <dependencies>
    <dependency>
      <groupId>org.osgi</groupId>
      <artifactId>osgi.core</artifactId>
      <version>${osgi.core.version}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>com.aicas.osgi.example</groupId>
      <artifactId>serviceloader-spi-example</artifactId>
      <version>${revision}</version>
    </dependency>
    <dependency>
      <groupId>com.aicas.osgi</groupId>
      <artifactId>serviceloader-mediator</artifactId>
      <version>${revision}</version>
    </dependency>
  </dependencies>
</project>
